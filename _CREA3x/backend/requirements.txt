fastapi==0.115.2
uvicorn[standard]==0.30.6
sqlmodel==0.0.22
SQLAlchemy>=2.0.14,<2.1.0
pydantic-settings==2.5.2
python-jose==3.3.0
python-multipart==0.0.9
passlib==1.7.4
email-validator==2.3.0
httpx==0.27.2
reportlab==4.2.2
python-dotenv==1.2.1
PyYAML==6.0.3
Pillow==12.0.0
openpyxl==3.1.5

# Knowledge Base (RAG): document parsing + indexing.
# The semantic ("FAISS") engine uses numpy exact inner-product cosine — identical
# results to faiss.IndexFlatIP for flat search, without faiss's native-wheel
# build risk. rank-bm25 is the keyword engine; the rest are pure-Python document
# parsers. All are imported lazily and degrade gracefully if missing.
numpy==2.1.3
rank-bm25==0.2.2
pypdf==5.1.0
python-docx==1.1.2
odfpy==1.4.1
