# ── Core runtime (what the served API actually imports) ───────────────────────
# The heavy local-AI stack (torch, transformers, sentence-transformers) and the
# offline evaluation tools live in requirements-local-ai.txt / -eval.txt: they
# add ~1.2 GB of downloads that this deployment never loads, and pulling them
# over a slow link is what breaks `pip install` mid-build.
python-dotenv==1.2.1
numpy==2.4.0
pandas==2.3.3
faiss-cpu==1.13.1

# Framework RAG e AI
langchain==1.2.0
langchain-openai
langchain-community==0.4.1
langchain-groq

tiktoken==0.12.0
uvicorn==0.38.0
fastapi==0.126.0
