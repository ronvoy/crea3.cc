fastapi==0.115.2
uvicorn[standard]==0.30.6
sqlmodel==0.0.22
SQLAlchemy>=2.0.14,<2.1.0
pydantic-settings==2.5.2
python-jose==3.3.0
python-multipart==0.0.9
passlib==1.7.4
email-validator==2.3.0
httpx==0.27.2
reportlab==4.2.2
python-dotenv==1.2.1
PyYAML==6.0.3
Pillow==12.0.0

# ── Legal RAG (see rag-plan.md) ────────────────────────────────────────────────
# numpy + pypdf + rank-bm25 are light and required for the baseline pipeline.
numpy>=1.26,<3.0
pypdf>=4.2.0
rank-bm25>=0.2.2
# Optional, higher-quality dense embeddings + ANN. The RAG service degrades
# gracefully (hashing vectorizer + numpy brute-force) when these are absent, so
# they are not hard requirements for the app to run.
# faiss-cpu>=1.8.0
# sentence-transformers>=3.0.0
