import React from 'react'

// Minimal design: the animated network background has been removed in favour of
// a flat Material surface. The page background colour is provided by the MUI
// theme (background.default) via CssBaseline. This component is kept as a no-op
// so existing imports continue to work.
export default function NetworkBackground(_props: { fixed?: boolean }) {
  return null
}
